{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {
      "persistent": true
    },
    "build": {
      "outputs": ["build/**", "dist/**"]
    },
    "//#lint": {
      "inputs": ["*"]
    },
    "//#format": {
      "inputs": ["*"],
      "outputs": ["*"]
    }
  }
}
